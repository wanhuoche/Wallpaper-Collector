{
  "name": "wallpaper-worker",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute wallpaper-db --file=src/schema.sql"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "jose": "^5.9.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
